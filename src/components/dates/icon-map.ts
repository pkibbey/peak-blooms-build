import type { ComponentType } from "react";
import { Bulb, Flame, InfoCircle, Stack } from "tabler-icons-react";

export const iconMap: Record<string, ComponentType<any>> = {
  Bulb,
  Flame,
  InfoCircle,
  Stack,
};
